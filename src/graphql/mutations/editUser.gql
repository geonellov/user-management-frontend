mutation (
      $id: ID!
      $firstName: String
      $lastName: String
      $email: String
){
    updateUser(
      id: $id,
      firstName: $firstName,
      lastName: $lastName,
      email: $email
    ){
      id
      firstName
    }
}